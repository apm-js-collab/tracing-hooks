// Emulates transpiler output targeting CommonJS (e.g. `tsc` with
// `module: commonjs`, as shipped in `@apollo/server/dist/cjs/*`): the
// "use strict" banner, the `__esModule` interop marker, a hoisted `require`,
// and `exports.default`. The transformer must inject a `require(...)` prelude
// here (not an `import`), or this module would no longer parse as CommonJS.
'use strict'
Object.defineProperty(exports, '__esModule', { value: true })
const os_1 = require('node:os')

class Foo {
  async doStuff() {
    return 'doing stuff' + os_1.EOL.slice(0, 0)
  }
}

exports.default = Foo
