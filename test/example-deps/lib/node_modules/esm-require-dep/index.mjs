// The nested import is what makes this package reproduce nodejs/node#59666 when it
// is reached through `require(esm)`: linking `./sub.mjs` needs the ESM loader, and on
// Node < 24.12 it is not in the synchronous loader's cache at that point.
import { suffix } from './sub.mjs'

export const value = `esm-require-dep${suffix}`
