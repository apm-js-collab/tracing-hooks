'use strict'
// Mirrors koa: an instrumented CommonJS main entry whose top-level require chain
// reaches an ES module (koa -> is-generator-function -> generator-function).
const { value } = require('esm-require-dep')

class Application {
  use(middleware) {
    return `${middleware}:${value}`
  }
}

module.exports = Application
